# ASGI and FastAPI
fastapi==0.110.2
uvicorn[standard]==0.29.0

# Redis async client
redis[asyncio]==5.0.4

# HTTP requests: async + sync
httpx==0.27.0
requests==2.32.2

# Utility for safequote (used with OAuth credentials)
kombu==5.3.5

# Environment variables (if used)
python-dotenv==1.0.1

# Airtable SDK (if used)
pyairtable==2.1.0

# Notion SDK (if used)
notion-client==2.2.1

# HubSpot SDK (if used)
hubspot-api-client==6.0.0
